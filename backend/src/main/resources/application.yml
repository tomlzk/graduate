server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/exam_service?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: tomlzk
    password: lvzhikang2004
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

jwt:
  secret: ExamServiceSecretKey2026ForJWTTokenGenerationAndValidation
  expiration: 86400000

logging:
  level:
    com.examservice: debug
